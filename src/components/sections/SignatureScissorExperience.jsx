'use client';

import React, { memo } from 'react';

/**
 * SignatureScissorExperience — Lightweight Placeholder
 * Temporarily disabled Heavy Three.js Canvas / Particles for 60 FPS performance optimization.
 */
function SignatureScissorExperience() {
  return (
    <div
      id="signature-scissor"
      className="relative w-full h-12 bg-transparent pointer-events-none select-none z-20"
      aria-hidden="true"
    />
  );
}

export default memo(SignatureScissorExperience);
